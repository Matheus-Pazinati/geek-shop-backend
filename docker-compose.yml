version: '3'

services:
  postgresql:
    image: bitnami/postgresql
    ports:
      - 5432:5432
    environment: 
      - POSTGRESQL_USERNAME=alurageek
      - POSTGRESQL_PASSWORD=alurageek
      - POSTGRESQL_DATABASE=geekshop